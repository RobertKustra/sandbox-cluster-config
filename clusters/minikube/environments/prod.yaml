apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: minikube-prod
  namespace: flux-system
spec:
  interval: 10m
  path: ./clusters/minikube/prod
  prune: true
  wait: true
  timeout: 5m0s
  sourceRef:
    kind: GitRepository
    name: sandbox-cluster-config
  postBuild:
    substitute:
      cluster: minikube
      environment: prod
