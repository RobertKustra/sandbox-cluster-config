apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: test
resources:
  - ../../base
patches:
  - target:
      kind: Ingress
      name: sandbox-app-ingress
    patch: |-
      - op: replace
        path: /spec/rules/0/host
        value: sandbox-app.test.local