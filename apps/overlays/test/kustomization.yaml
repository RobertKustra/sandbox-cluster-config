apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: test
resources:
  - ../../base
secretGenerator:
  - name: redis-auth
    literals:
      - redis-password=test-change-me
generatorOptions:
  disableNameSuffixHash: true
patches:
  - target:
      kind: Ingress
      name: sandbox-app-ingress
    patch: |-
      - op: replace
        path: /spec/rules/0/host
        value: sandbox-app.test.local