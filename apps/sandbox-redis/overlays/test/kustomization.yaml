apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: test
resources:
  - ../../base
secretGenerator:
  - name: sandbox-redis-auth
    literals:
      - sandbox-redis-password=<password-placeholder>
generatorOptions:
  disableNameSuffixHash: true