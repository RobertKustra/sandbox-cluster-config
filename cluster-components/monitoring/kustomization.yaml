apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
configMapGenerator:
  - name: prometheus-alert-rules
    files:
      - values.yaml=alert-rules.yaml
  - name: grafana-dashboards
    files:
      - values.yaml=grafana-dashboards-values.yaml
generatorOptions:
  disableNameSuffixHash: true
resources:
  - ./namespace.yaml
  - ./prometheus.yaml
  - ./grafana.yaml
  - ./jaeger.yaml
  - ./gpu-exporter.yaml
  - ./promtail.yaml
  - ./loki-stack.yaml
  - ./alertmanager.yaml
  - ./monitoring-ingress.yaml
