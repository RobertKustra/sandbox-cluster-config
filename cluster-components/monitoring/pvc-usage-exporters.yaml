apiVersion: batch/v1
kind: CronJob
metadata:
  name: pvc-usage-grafana
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      activeDeadlineSeconds: 300
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: pvc-usage-exporter
              image: alpine:3.22
              command: ["/bin/sh", "-c"]
              args:
                - |
                  used_kib=$(du -sk /volume | awk '{print $1}')
                  used_bytes=$((used_kib * 1024))
                  wget -qO- \
                    --header='Content-Type: text/plain; version=0.0.4' \
                     --post-data="pvc_directory_used_bytes $used_bytes" \
                     "http://prometheus-prometheus-pushgateway.monitoring.svc:9091/metrics/job/pvc-directory-usage/namespace/$POD_NAMESPACE/persistentvolumeclaim/$PVC_NAME"
              env:
                - name: POD_NAMESPACE
                  valueFrom:
                    fieldRef:
                      fieldPath: metadata.namespace
                - name: PVC_NAME
                  value: grafana
              resources:
                requests:
                  cpu: 10m
                  memory: 16Mi
                limits:
                  cpu: 200m
                  memory: 64Mi
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
              volumeMounts:
                - name: volume
                  mountPath: /volume
                  readOnly: true
          volumes:
            - name: volume
              persistentVolumeClaim:
                claimName: grafana
                readOnly: true
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: pvc-usage-prometheus-server
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      activeDeadlineSeconds: 300
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: pvc-usage-exporter
              image: alpine:3.22
              command: ["/bin/sh", "-c"]
              args:
                - |
                  used_kib=$(du -sk /volume | awk '{print $1}')
                  used_bytes=$((used_kib * 1024))
                  wget -qO- \
                    --header='Content-Type: text/plain; version=0.0.4' \
                     --post-data="pvc_directory_used_bytes $used_bytes" \
                     "http://prometheus-prometheus-pushgateway.monitoring.svc:9091/metrics/job/pvc-directory-usage/namespace/$POD_NAMESPACE/persistentvolumeclaim/$PVC_NAME"
              env:
                - name: POD_NAMESPACE
                  valueFrom:
                    fieldRef:
                      fieldPath: metadata.namespace
                - name: PVC_NAME
                  value: prometheus-server
              resources:
                requests:
                  cpu: 10m
                  memory: 16Mi
                limits:
                  cpu: 200m
                  memory: 64Mi
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
              volumeMounts:
                - name: volume
                  mountPath: /volume
                  readOnly: true
          volumes:
            - name: volume
              persistentVolumeClaim:
                claimName: prometheus-server
                readOnly: true
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: pvc-usage-alertmanager
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      activeDeadlineSeconds: 300
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: pvc-usage-exporter
              image: alpine:3.22
              command: ["/bin/sh", "-c"]
              args:
                - |
                  used_kib=$(du -sk /volume | awk '{print $1}')
                  used_bytes=$((used_kib * 1024))
                  wget -qO- \
                    --header='Content-Type: text/plain; version=0.0.4' \
                     --post-data="pvc_directory_used_bytes $used_bytes" \
                     "http://prometheus-prometheus-pushgateway.monitoring.svc:9091/metrics/job/pvc-directory-usage/namespace/$POD_NAMESPACE/persistentvolumeclaim/$PVC_NAME"
              env:
                - name: POD_NAMESPACE
                  valueFrom:
                    fieldRef:
                      fieldPath: metadata.namespace
                - name: PVC_NAME
                  value: storage-alertmanager-0
              resources:
                requests:
                  cpu: 10m
                  memory: 16Mi
                limits:
                  cpu: 200m
                  memory: 64Mi
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
              volumeMounts:
                - name: volume
                  mountPath: /volume
                  readOnly: true
          volumes:
            - name: volume
              persistentVolumeClaim:
                claimName: storage-alertmanager-0
                readOnly: true
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: pvc-usage-prometheus-alertmanager
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      activeDeadlineSeconds: 300
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: pvc-usage-exporter
              image: alpine:3.22
              command: ["/bin/sh", "-c"]
              args:
                - |
                  used_kib=$(du -sk /volume | awk '{print $1}')
                  used_bytes=$((used_kib * 1024))
                  wget -qO- \
                    --header='Content-Type: text/plain; version=0.0.4' \
                     --post-data="pvc_directory_used_bytes $used_bytes" \
                     "http://prometheus-prometheus-pushgateway.monitoring.svc:9091/metrics/job/pvc-directory-usage/namespace/$POD_NAMESPACE/persistentvolumeclaim/$PVC_NAME"
              env:
                - name: POD_NAMESPACE
                  valueFrom:
                    fieldRef:
                      fieldPath: metadata.namespace
                - name: PVC_NAME
                  value: storage-prometheus-alertmanager-0
              resources:
                requests:
                  cpu: 10m
                  memory: 16Mi
                limits:
                  cpu: 200m
                  memory: 64Mi
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
              volumeMounts:
                - name: volume
                  mountPath: /volume
                  readOnly: true
          volumes:
            - name: volume
              persistentVolumeClaim:
                claimName: storage-prometheus-alertmanager-0
                readOnly: true
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: pvc-usage-loki
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      activeDeadlineSeconds: 300
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: pvc-usage-exporter
              image: alpine:3.22
              command: ["/bin/sh", "-c"]
              args:
                - |
                  used_kib=$(du -sk /volume | awk '{print $1}')
                  used_bytes=$((used_kib * 1024))
                  wget -qO- \
                    --header='Content-Type: text/plain; version=0.0.4' \
                    --post-data="pvc_directory_used_bytes $used_bytes" \
                    "http://prometheus-prometheus-pushgateway.monitoring.svc:9091/metrics/job/pvc-directory-usage/namespace/$POD_NAMESPACE/persistentvolumeclaim/$PVC_NAME"
              env:
                - name: POD_NAMESPACE
                  valueFrom:
                    fieldRef:
                      fieldPath: metadata.namespace
                - name: PVC_NAME
                  value: storage-loki-stack-0
              resources:
                requests:
                  cpu: 10m
                  memory: 16Mi
                limits:
                  cpu: 200m
                  memory: 64Mi
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
              volumeMounts:
                - name: volume
                  mountPath: /volume
                  readOnly: true
          volumes:
            - name: volume
              persistentVolumeClaim:
                claimName: storage-loki-stack-0
                readOnly: true